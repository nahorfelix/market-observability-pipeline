version: '3.8'
services:
  backend-api:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production

  frontend-dashboard:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend-api